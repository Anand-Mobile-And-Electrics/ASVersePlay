import { AuthWidget } from "@/components/auth-widget";

export default function AuthPage() {
  return (
    <main className="grid min-h-[calc(100vh-73px)] place-items-center px-6 py-10">
      <AuthWidget />
    </main>
  );
}
